{{- include "common.service" (list . "this.service") }}
{{- define "this.service" }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
{{- end }}