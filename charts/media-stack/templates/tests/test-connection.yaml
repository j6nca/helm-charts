apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "media-stack.fullname" . }}-test-connection"
  labels:
    {{- include "media-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "media-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
